from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.0.1',
    description='This is a small project to find the rotation angle of a given image using neural network',
    author='Liev Garcia',
    license='MIT',
)
